{
  "name": "@rtfm/contracts",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "forge test",
    "build": "forge build",
    "deploy:sepolia": "forge script script/Deploy.s.sol --rpc-url $SEPOLIA_RPC_URL --broadcast --verify -vvv",
    "typecheck": "echo 'Solidity contracts do not require typecheck'"
  },
  "devDependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "forge-std": "^1.1.2",
    "solhint": "^6.0.3"
  }
}
